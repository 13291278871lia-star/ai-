# AI 工作台源码交接说明

此项目是本地网页应用，不能直接双击 `index.html`。解压后需要启动本地服务：

- macOS：双击 `啟動-AI工作台.command`
- Windows：双击 `启动-AI工作台-Windows.bat`
- 命令行：运行 `python3 serve.py`，然后打开 `http://127.0.0.1:4175/`

主要修改位置：

- `src/main.js`：导航、页面内容和资料栏目
- `src/style.css`：颜色、排版和响应式样式
- `src/insurance-*.js`：PPT、IRR 折线图与文件导入
- `serve.py`：本地服务和资料读取接口
- `assets/vendor/`：项目运行所需的浏览器端组件

源码交接包不包含内部产品 PDF、推广文件及培训视频。完整项目中的这些资料位于：

- `assets/library/`
- `assets/market-info/`
- `assets/discontinued/`
- `assets/promotion/`
- `assets/training/`
- `assets/ppt-reference/`

如需在另一台电脑还原全部资料，请保持上述目录结构，将资料文件夹另行复制进项目的 `assets/` 目录。

建议使用私人代码仓库。不要把含内部资料的完整项目上传到公开仓库。
